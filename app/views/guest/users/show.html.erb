
<div class="min-h-screen bg-[#F6F5EE] p-6">
  <div class="w-full max-w-md">
    <%# フラッシュメッセージ %>
    <%= render "shared/flash", flash: flash %>

    <%# 経験値UP・レベルアップなど %>
    <%= render "shared/event_message", events: status_events %>

    <%= render "shared/user_card", user: @user, user_status: @user_status %>

    <%# やりたいこと一覧 %>
    <%= render "wants/index", wants: @wants %>
  </div>
</div>
